# 贡献指南

本仓库当前以单人主导开发为主，但所有后续改动都应遵循一致的提交和审阅规则。

## 基本原则

- 先补设计，再做实现
- 先确认边界，再扩大功能
- 优先小步提交，避免一次性混入太多不相关改动
- 不要把本地敏感配置、令牌、运行时数据提交进仓库

## 分支与提交

- 默认分支为 `main`
- 功能开发建议从独立分支发起
- 提交信息保持清晰，推荐使用短前缀，例如：
  - `feat:`
  - `fix:`
  - `docs:`
  - `chore:`
  - `refactor:`

## 代码变更要求

- 新功能至少附带最小可验证路径
- 配置变更要说明影响范围
- 任何涉及权限、审批、凭据的逻辑都需要明确说明风险
- 对外行为有变化时，需要同步更新 README 或相关说明

## 提交前检查

在提交前至少执行：

```bash
npm run check
npm test
```

如果改动影响运行路径，建议补充：

```bash
npm run dev
```

## 文档约定

- 仓库首页 README 采用英文默认、中文跳转
- 说明性文档优先中文
- `docs/` 目录不再纳入远端仓库版本管理
